# Tasks

## 1. Foundation documentation

- [x] Update `AGENTS.md` with the initial stack, quality gate, and
  engineering policies
- [x] Update `PROJECT_CONTEXT.md` with the system goal, architecture,
  and current state
- [x] Record the stack decision and principles in an ADR
- [x] Create the first change in `openspec`

## 2. Initial Python scaffold

- [x] Create `pyproject.toml` and the initial Python validation
  commands
- [x] Add minimal pytest, Ruff, Pyright, and markdown tooling
  configuration
- [x] Encode an initial module with unit tests for architectural
  boundaries
- [x] Validate `bash scripts/markdown-lint.sh`, `uv run pytest`,
  `uv run ruff check .`, and `uv run pyright`

## 3. Next expected slice

- [x] Specify the memory layer foundation
- [x] Realign the stack direction for the Hermes-centered path
- [x] Design the VeraBrain core and adapter boundaries
