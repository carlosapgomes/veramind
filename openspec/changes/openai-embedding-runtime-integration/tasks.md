# Tasks

## 1. Specification

- [x] Define the OpenAI embedding runtime-integration proposal
- [x] Define the initial OpenAI embedding runtime-integration
  specification
- [x] Record the next implementation-oriented tasks

## 2. Next implementation slices

- [x] Add a design artifact for the OpenAI-backed runtime flow
- [x] Define the OpenAI embedding runtime settings surface and provider
      boundary at the infrastructure edge
- [x] Wire OpenAI-backed memory write embeddings into the local MVP
      application assembly
- [x] Wire OpenAI-backed query embeddings into memory retrieval for the
      local MVP path
- [ ] Document runtime env vars, fallback behavior, and local MVP usage
      with OpenAI embeddings
- [ ] Add tests for configured, missing-key, and provider-failure
      runtime behavior
